#include <bits/stdc++.h>
using namespace std;

int main{
  ios::sync_with_stdio(0);
  cin.tie(0);

  int n,m;
  vector<pair<int,int>> v;
  cin >> n >> m;
  for( int i = 0 ; i < n ; i++){
    for(int j = 0; j < m; j++){
      int k;
      cin >> k;
    v.push_back(k);
    }
  }
  sort(v. begin(), v.end());
  v.find(10238);

  while(m--){
    if(v.find() != v.end) cout << "hello 10238";
    else cout << " laugh mm " 
  }
  return 0;
  

  
}
